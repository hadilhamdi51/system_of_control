

<h2>user details</h2>
<div class="card mb-3">
    <div class="card-body">
      <h5 class="card-title">{{ $user->name }}<span class="badge bg-primary">{{ $user->name }}</span></h5>
      <h5 class="card-title">{{ $user->email }}<span class="badge bg-primary">{{ $user->email}}</span></h5><br>
      
    </div>
  </div>

